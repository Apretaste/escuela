<div class="row" style="margin-bottom: 6px;">
	<div class="col s12">
		<h1>Bienvenido <%= name %></h1>
        <% if (completed == 0) {%> <!-- si no esta registrado en ningun curso debe decir ... -->
        <p>Escoja un curso para empezar a estudiar, ganar niveles y medallas.</p>
        <% } else { %>
		<p>Usted ha completado <%= completed %> cursos y tiene <b><%= level %></b></p>
		<p>A continuaci&oacute;n sus cursos empezados.</p>
        <% }%>
	</div>
</div>
<div class="row">
<% courses.forEach(function(e, i) { %>
	<div class="col s12 m6">
		<div class="card">
			<div class="card-content">
				<span class="card-title"><%= e.title %></span>
				<div class="progress">
					<div class="determinate" style="width: <%= e.progress %>%"></div>
				</div>
				<p>
					Por <a href="#" class="link link-simple" data-command = "ESCUELA BUSCAR" data-query="{author: courses[<%= i %>].teacher}"><%= e.professor %></a>
					<%if (e.category != null){%> ● 
					<a href="#" class="link link-simple" data-command = "ESCUELA BUSCAR" data-query="{category: courses[<%= i %>].category}"><%= jsUcfirst(e.category.toLowerCase()) %></a>
					<%}%>
				</p>
 				<p class="space5">
					<% for(var k = 0; k < e.stars; k++) {%>
					<i class="material-icons">star</i>
					<% } %>
                    <% for(var k = 0; k < max_stars - e.stars; k++) {%>
					<i class="material-icons">star_border</i>
					<% } %>
				</p>
			</div>
			<div class="card-reveal">
				<span class="card-title grey-text text-darken-4">
					<i class="material-icons right">close</i>
				</span>
				<p><%= e.content %></p>
			</div>
			<div class="card-action">
				<a href="#!" class="link link-simple" data-command="ESCUELA CURSO" data-query="<%= e.id %>">Ir al curso</a>
				<i class="material-icons card-title right activator">more_horiz</i>
			</div>
		</div>
	</div>
<% }); %>
</div>
